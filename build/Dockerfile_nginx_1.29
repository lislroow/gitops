FROM nginx:1.29

# - ping (iputils)
# - dig, nslookup (dnsutils)
# - ip/route (iproute2)
RUN apt-get update && \
    apt-get install -y iputils-ping dnsutils iproute2 && \
    rm -rf /var/lib/apt/lists/*

CMD ["nginx", "-g", "daemon off;"]
