services:
  mariadb-market-inventory:
    image: mariadb:11.5.2
    container_name: mariadb-market-inventory
    ports:
      - 3403:3403
    restart: always
    environment:
      - TZ: Asia/Seoul
      - MYSQL_HOST=localhost
      - MYSQL_TCP_PORT=3306
      - MYSQL_ROOT_PASSWORD=1
      - MYSQL_DATABASE=market-inventory
      - MYSQL_USER=mkuser
      - MYSQL_PASSWORD=1
    command:
      - --log-bin
      - --log-basename=log_mariadb
      - --binlog_format=ROW
    volumes:
      - mariadb-market-inventory_data:/var/lib/mysql
    networks:
      - local-default

volumes:
  mariadb-market-inventory_data:

networks:
  local-default:
    external: true
